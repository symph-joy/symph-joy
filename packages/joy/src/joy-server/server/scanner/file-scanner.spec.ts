import { FileScanner } from "./file-scanner";
import * as path from "path";

describe("joy", () => {
  test("JoyBoot is a container", async () => {
    // const scanner = new FileScanner()
    // await scanner.scan(path.resolve(__dirname, './fixtures'))
  });
});
