import { MountModule } from "@symph/server";

{{#modules}}
import * as m{{@index}} from {{json path}}
{{/modules}}

export const ___JOY_GEN_PROVIDERS = [
{{#modules}}
  {
    path: {{json path}},
    {{#if mount}}mount: {{json mount}},{{/if}}
    module: m{{@index}},
    //module: require({{json path}}),
  },
{{/modules}}
]

if (module.hot) {
  module.hot.accept();
}




