export const joyReactAutoGenRoutes = {
  id: "joyReactAutoGenRoutes",
  type: Array,
  useValue: [
    {{#routes}}
    {
      path: "{{path}}",
      providerId: "{{providerId}}",
      component: {{#if dynamic}}()=>import({{json filePath}}){{else}}require({{json filePath}}){{/if}},
    },
    {{/routes}}
  ]
}
